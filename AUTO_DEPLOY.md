# 🚀 一键自动配置 - GitHub + Netlify

## ⚡ 最快方式（推荐）

只需运行一个命令，自动完成所有配置！

```bash
./quick-setup.sh
```

### 脚本会做什么？

✅ **自动打开浏览器**到 GitHub 创建页面（预填表单）
✅ **自动推送代码**到 GitHub
✅ **自动打开浏览器**到 Netlify 配置页面
✅ **自动测试**自动部署
✅ **显示所有重要链接**

### 你需要做什么？

1. 在打开的浏览器中确认创建 GitHub 仓库
2. 在 Netlify 页面点击 "Connect to GitHub"
3. 等待自动部署完成

**总耗时**: 约 3-5 分钟

---

## 📋 详细步骤说明

### 第 1 步：创建 GitHub 仓库

脚本会自动打开浏览器到：
```
https://github.com/new?name=news-cli&description=...&visibility=public
```

**你只需要**:
1. 确认仓库名称是 `news-cli`
2. 确认描述已填写
3. 选择 `Public`
4. **不要勾选**任何选项（README, .gitignore, license）
5. 点击绿色按钮 **"Create repository"**

然后回到终端按回车。

### 第 2 步：推送代码

脚本自动执行：
```bash
git push -u origin main
```

你会在终端看到推送进度。

### 第 3 步：配置 Netlify

脚本会自动打开 Netlify 设置页面。

**你只需要**:
1. 找到 **"Continuous Deployment"** → **"GitHub"**
2. 点击 **"Connect to GitHub"**
3. 选择 `news-cli` 仓库
4. 确认配置：
   - Publish directory: `public`
   - Functions directory: `netlify/functions`
5. 点击 **"Save"**

然后回到终端按回车。

### 第 4 步：测试自动部署

脚本会自动创建一个测试提交并推送。

访问提供的链接查看部署状态。

---

## 🎯 完成后

### 日常使用

修改代码后，只需运行：

```bash
./push.sh
```

输入提交信息，Netlify 会自动部署！

### 提交信息格式

```bash
./push.sh
# 输入: feat: 添加新功能
# 输入: fix: 修复bug
# 输入: docs: 更新文档
```

---

## 🔗 重要链接

配置完成后保存这些链接：

- **GitHub 仓库**: https://github.com/yaweilee/news-cli
- **部署日志**: https://app.netlify.com/projects/charming-dolphin-4ef2cb/deploys
- **网站地址**: https://charming-dolphin-4ef2cb.netlify.app
- **Netlify 设置**: https://app.netlify.com/projects/charming-dolphin-4ef2cb/settings

---

## 🛠️ 其他自动化脚本

### `auto-setup.sh` (带 GitHub Token)

如果你有 GitHub Personal Access Token，可以使用这个脚本：

```bash
./auto-setup.sh
```

优点：
- 可以通过 API 自动创建仓库
- 完全自动化流程

需要：
- GitHub Token (需要有 repo 权限)

### `push.sh` (日常使用)

每次修改代码后使用：

```bash
./push.sh
```

功能：
- 自动添加所有更改
- 创建提交
- 推送到 GitHub
- 触发 Netlify 自动部署

### `deploy.sh` (手动部署)

手动部署到 Netlify（不经过 GitHub）：

```bash
./deploy.sh
```

---

## ⚙️ 高级配置

### 添加 GitHub Token（可选）

如果你想完全自动化（包括创建仓库），可以：

1. 生成 GitHub Token:
   - 访问: https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 勾选 `repo` 权限
   - 生成并复制 token

2. 运行脚本:
   ```bash
   ./auto-setup.sh
   # 输入你的 token
   ```

### 配置 Git 认证

如果推送时提示输入密码：

```bash
# 方式 1: 使用 SSH (推荐)
git remote set-url origin git@github.com:yaweilee/news-cli.git

# 方式 2: 使用 GitHub CLI
brew install gh
gh auth login

# 方式 3: 使用 Personal Access Token
# 在密码处输入 token 而不是 GitHub 密码
```

---

## ❓ 常见问题

### Q1: 浏览器没有自动打开？

**A**: 手动访问这些链接：

创建仓库:
```
https://github.com/new?name=news-cli&description=中国热门新闻CLI工具&visibility=public
```

Netlify 设置:
```
https://app.netlify.com/projects/charming-dolphin-4ef2cb/settings/deploys
```

### Q2: git push 失败？

**A**: 检查：
1. GitHub 仓库是否已创建
2. 仓库名称是否正确 (`news-cli`)
3. 网络连接是否正常

### Q3: Netlify 找不到仓库？

**A**:
1. 确保仓库是 Public
2. 在 Netlify 重新授权 GitHub
3. 刷新 Netlify 页面

---

## ✅ 检查清单

配置完成后检查：

- [ ] GitHub 仓库已创建并可访问
- [ ] 代码已推送到 GitHub
- [ ] Netlify 已连接 GitHub
- [ ] 测试提交已触发部署
- [ ] 网站可以正常访问
- [ ] `./push.sh` 可以正常使用

---

## 📚 相关文档

- **README.md** - 项目说明
- **QUICKSTART.md** - 快速开始
- **GITHUB_SETUP.md** - 详细配置指南
- **NETLIFY_DEPLOY.md** - Netlify 部署详解

---

## 🎉 准备好了吗？

运行一键配置脚本：

```bash
./quick-setup.sh
```

3-5 分钟后，享受自动化开发！ 🚀
